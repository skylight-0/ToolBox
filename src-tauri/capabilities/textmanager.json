{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "textmanager-capability",
  "description": "Capability for the standalone text manager window",
  "windows": ["textmanager"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging"
  ]
}
